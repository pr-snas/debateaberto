<% publish_to "/#{@canal.id}/mensagens/new" do %>
  $elem = $("#chat");
  $elem.append("<%= j render(@mensagem) %>");
  $elem.scrollTop($elem[0].scrollHeight);
<% end %>
$("#mensagem_content").val('');
